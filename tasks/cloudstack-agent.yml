---
- name: "APT: Install cloudstack-agent"
  ansible.builtin.apt:
    pkg:
      - "cloudstack-agent"
      - "ovmf"
      - "sysfsutils"
    state: latest
  notify: cloudstack_agent_restart

- name: Create the cloudstack user
  ansible.builtin.user:
    name: "cloudstack"
    create_home: true
    shell: "/bin/bash"
    state: present
    password: "!"
    update_password: always

- name: Update sudoers for cloudstack agent setup requiretty
  ansible.builtin.lineinfile:
    path: /etc/sudoers
    line: "Defaults:cloudstack     !requiretty"
    insertafter: "^#Defaults:.*"

- name: Update sudoers for cloudstack agent setup nopasswd
  ansible.builtin.lineinfile:
    path: /etc/sudoers
    line: "cloudstack ALL=NOPASSWD: {{ item }}"
    insertafter: "^root.*"
  with_items:
    - "/usr/bin/cloudstack-setup-agent"
    - "/usr/share/cloudstack-common/scripts/util/keystore-setup"
    - "/usr/share/cloudstack-common/scripts/util/keystore-cert-import"
    - "/usr/sbin/service"
    - "/usr/sbin/update-rc.d"

- name: See if public key is registered
  ansible.builtin.stat:
    path: /home/cloudstack/.ssh/authorized_keys
  register: authorized_keys

- name: Fetch management node public key
  ansible.builtin.slurp:
    src: /var/lib/cloudstack/management/.ssh/id_rsa.pub
  register: public_key
  delegate_to: "{{ groups['cloudstack_mgmt'] | first }}"
  when: not authorized_keys.stat.exists

- name: "Ensure /home/cloudstack/.ssh exists"
  ansible.builtin.file:
    path: "/home/cloudstack/.ssh/"
    state: directory
    mode: "0700"
    owner: "cloudstack"
    group: "cloudstack"

- name: "Make sure the public key for ssh authentication is configured for the cloudstack user"
  ansible.builtin.lineinfile:
    path: "/home/cloudstack/.ssh/authorized_keys"
    state: present
    create: true
    owner: cloudstack
    group: cloudstack
    mode: "0600"
    line: "{{ public_key['content'] | b64decode }}"
  when: not authorized_keys.stat.exists

- name: "Calculate reserved memory"
  ansible.builtin.set_fact:
    reserved_mem: >-
      {%- set ceph_mon   = (2048 if inventory_hostname in groups["ceph_" + ceph_cluster_name|default("") + "_mon"]|default([]) else 0) %}
      {%- set ceph_mds   = (2048 if inventory_hostname in groups["ceph_" + ceph_cluster_name|default("") + "_mds"]|default([]) else 0) %}
      {#- This assumes 3 OSDs per host @ 2G ea, really should count number of OSDs -#}
      {%- set ceph_osd   = (6144 if inventory_hostname in groups["ceph_" + ceph_cluster_name|default("") + "_osd"]|default([]) else 0) %}
      {%- set mariadb    = (mariadb_buffer_pool_size|default(4096) if mariadb_cluster_name is not defined else 0) %}
      {%- set cloudstack = (2048 if inventory_hostname in groups["cloudstack_mgmt"]|default("") else 0) %}
      {{- ceph_mon + ceph_mds + ceph_osd + mariadb + cloudstack -}}

- name: "cloudstack enable UEFI in agent"
  ansible.builtin.copy:
    src: uefi.properties
    dest: /etc/cloudstack/agent/uefi.properties
    mode: "644"
    owner: root
  notify: cloudstack_agent_restart

- name: "Enable nvram UEFI settings for libvirt qemu"
  ansible.builtin.blockinfile:
    path: /etc/libvirt/qemu.conf
    marker: "# {mark} ANSIBLE MANAGED NVRAM BLOCK"
    insertbefore: "#nvram"
    block: |
      nvram = [
        "/usr/share/OVMF/OVMF_CODE_4M.fd:/usr/share/OVMF/OVMF_VARS_4M.fd",
        "/usr/share/OVMF/OVMF_CODE_4M.secboot.fd:/usr/share/OVMF/OVMF_VARS_4M.fd",
        "/usr/share/OVMF/OVMF_CODE_4M.ms.fd:/usr/share/OVMF/OVMF_VARS_4M.ms.fd"
      ]
  notify: libvirt_restart

- name: "cloudstack agent set cpu model"
  ansible.builtin.lineinfile:
    path: /etc/cloudstack/agent/agent.properties
    regexp: "^#?{{ item.key | replace('.', '\\.') }} ?= ?.*"
    line: "{{ item.key }}={{ item.val }}"
  with_items:
    - { key: "hypervisor.type", val: "kvm" }
    - { key: "guest.cpu.mode", val: "host-model" }
    - { key: "vm.memballoon.disable", val: "false" }
    - { key: "vm.memballoon.stats.period", val: "10" }
    - { key: "libvirt.events.enabled", val: "true" }
    - { key: "host.reserved.mem.mb", val: "{{ reserved_mem }}" }
    - { key: "vm.rng.enable", val: "true" }
  notify: cloudstack_agent_restart

- name: "See if cloudstack TLS key has been created"
  ansible.builtin.stat:
    path: /etc/cloudstack/agent/cloud.key
  register: cloudstack_agent_key

- name: "setup libvirt for cloudstack"
  ansible.builtin.lineinfile:
    path: /etc/libvirt/libvirtd.conf
    line: "{{ item.key }} = {{ item.val }}"
    regexp: "^#?{{ item.key }} *=.*"
  with_items:
    - { key: "listen_tls", val: "{{ '1' if cloudstack_agent_key.stat.exists else '0' }}" }
    - { key: "listen_tcp", val: "0" }
    - { key: "tls_port", val: '"16514"' }
    - { key: "tcp_port", val: '"16509"' }
    - { key: "auth_tcp", val: '"none"' }
    - { key: "mdns_adv", val: "0" }
  notify: libvirt_restart

- name: "setup libvirt for cloudstack /etc/default"
  ansible.builtin.lineinfile:
    path: /etc/default/libvirtd
    line: 'LIBVIRTD_ARGS="--listen"'
    regexp: "^#?LIBVIRTD_ARGS=.*"
  notify: libvirt_restart

- name: "setup libvirt for cloudstack"
  ansible.builtin.lineinfile:
    path: /etc/libvirt/libvirt.conf
    line: 'remote_mode="legacy"'
    regexp: "^#?remote_mode *=.*"

- name: "Mask socket services"
  ansible.builtin.systemd_service:
    name: "{{ item }}"
    masked: true
    enabled: false
    state: stopped
  with_items:
    - libvirtd.socket
    - libvirtd-ro.socket
    - libvirtd-admin.socket
    - libvirtd-tls.socket
    - libvirtd-tcp.socket
  notify: libvirt_restart

- name: "Create symlinks to disable apparmor"
  ansible.builtin.file:
    state: link
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
  with_items:
    - { src: "/etc/apparmor.d/usr.sbin.libvirtd", dest: "/etc/apparmor.d/disable/usr.sbin.libvirtd" }
    - { src: "/etc/apparmor.d/usr.lib.libvirt.virt-aa-helper", dest: "/etc/apparmor.d/disable/usr.lib.libvirt.virt-aa-helper" }
  register: disable_apparmor

- name: "Load apparmor profile"
  ansible.builtin.shell: "apparmor_parser -R /etc/apparmor.d/usr.sbin.libvirtd && apparmor_parser -R /etc/apparmor.d/usr.lib.libvirt.virt-aa-helper"
  when: disable_apparmor.changed

- name: "Configure for vxlan evpn"
  ansible.builtin.copy:
    src: modifyvxlan-evpn.sh
    dest: /usr/share/modifyvxlan.sh
    mode: "755"

- name: "UFW: Load bridge module"
  ansible.builtin.lineinfile:
    path: /etc/default/ufw
    line: 'IPT_MODULES="bridge"'
    regexp: "^IPT_MODULES"
  notify: ufw_restart

- name: "UFW: Don't apply rules on bridge traffic"
  ansible.builtin.lineinfile:
    path: /etc/ufw/sysctl.conf
    line: "{{ item.key }} = {{ item.val }}"
    regexp: "^{{ item.key }}.*"
  with_items:
    - { key: net.bridge.bridge-nf-call-ip6tables, val: 0 }
    - { key: net.bridge.bridge-nf-call-iptables, val: 0 }
    - { key: net.bridge.bridge-nf-call-arptables, val: 0 }
  notify: ufw_restart

- name: "UFW: Enable Cloudstack Agent Ports"
  community.general.ufw:
    rule: allow
    port: "{{ item }}"
    proto: tcp
  with_items:
    - "1798"
    - "16514" # libvirt
    - "5900:6100" # VNC consoles)
    - "49152:49216" # libvirt live migration

# KSM dedup lets us overcommit guest memory, but an aggressive scan rate
# (pages_to_scan / sleep_millisecs) forces constant THP split/collapse churn.
# That churn, together with synchronous THP compaction, drove multi-CPU soft
# lockups in the KVM vCPU threads. Scan rate here is ~5k pages/s (1000/200ms)
# vs the previous ~80k pages/s (4000/50ms) -- still dedups, far less lock/CPU
# churn.
- name: "KSM: dedup settings (moderated scan rate)"
  ansible.builtin.copy:
    dest: /etc/sysfs.d/ksm.conf
    mode: "644"
    owner: root
    content: |
      kernel/mm/ksm/run = 1
      kernel/mm/ksm/sleep_millisecs = 200
      kernel/mm/ksm/pages_to_scan = 1000
  notify: sysfs_restart

# Guest RAM is THP-backed (madvise(MADV_HUGEPAGE) from QEMU). Any defrag mode
# that includes "madvise" -- including defer+madvise -- still does SYNCHRONOUS
# direct compaction for those madvise regions, i.e. inside the faulting vCPU
# thread. On a fragmented, overcommitted host that compaction stalls the vCPU
# for 200s+ and trips the soft-lockup watchdog (observed with compact_stall in
# the hundreds of thousands, 61% of them failing, when an allocation burst in
# one guest wedged vCPUs across the host).
#
# defrag=never removes direct compaction from the fault path entirely: a THP
# allocation that cannot be satisfied from free memory falls back to 4 KiB
# instantly (no stall). enabled=madvise still lets guests use huge pages when
# they are freely available, and khugepaged still collapses them in the
# BACKGROUND (its own thread, never the vCPU). Trade: slightly more 4 KiB
# fallback / TLB pressure -- worth it to eliminate the soft-lockup mechanism.
- name: "THP: keep huge pages but never compact in the vCPU fault path"
  ansible.builtin.copy:
    dest: /etc/sysfs.d/thp.conf
    mode: "644"
    owner: root
    content: |
      kernel/mm/transparent_hugepage/enabled = madvise
      kernel/mm/transparent_hugepage/defrag = never
  notify: sysfs_restart

#- name: "BUG: Workaround agent issues by restarting daily"
#  template:
#    src: cloudstack-agentrestart.cron
#    dest: /etc/cron.d/cloudstack-agentrestart
#    owner: root
#    mode: 0644

- name: "Flush handlers"
  ansible.builtin.meta: flush_handlers
